/*In this project we will be learning about the TextFormFile in Flutter*/

This is somewhat similar to the Text Field but with a little more properties and features
TextForm field is suitable for forms and provides extra features like validation

One of the Major difference between TextField and TextFormField is TextFiled uses controller to validate whereas the TextFormFiled uses Validator

For simple text inputs use TextField and for the complex ones go with the TextFormField